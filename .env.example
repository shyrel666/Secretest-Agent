# Server
PORT=10929
HOSTNAME=localhost

# Optional: set to true only for single-user local deployments.
# Keep false for LAN/shared deployments to avoid sharing one local question-bank identity.
QUESTION_BANK_SINGLE_USER_MODE=false
